<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.dpad"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Dibutuhkan untuk menyimpan PDF ke Downloads di Android 9 (API 28) ke bawah -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature android:name="android.hardware.camera.any"
        android:required="false" />

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="34"
        tools:overrideLibrary="com.alexvas.rtsp" />

    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="org.telegram.messenger" />
        <package android:name="com.google.android.gm" />

        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="video/*" />
        </intent>
    </queries>

    <!--
        FIX #1 — android:usesCleartextTraffic hanya boleh ada SEKALI sebagai
        atribut <application>, bukan sebagai teks di dalam body elemen.
        Versi lama punya duplikat orphan teks yang bikin manifest corrupt.
    -->
    <application
        android:allowBackup="true"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">

        <!--
            FIX #2 — SplashActivity tetap launcher.
            Pengecekan token (Login vs Home) dilakukan di dalam SplashActivity.kt.
        -->
        <activity
            android:name=".auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme_App_Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FIX #3 — LoginActivity ditambahkan (bukan launcher, dipanggil dari SplashActivity) -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <!--
            FIX #4 — HomeActivity cukup dideklarasikan SEKALI.
            Versi lama mendeklarasikannya dua kali (exported=false dan exported=true)
            yang menyebabkan duplicate activity declaration warning/error.
        -->
        <activity
            android:name=".home.HomeActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".auth.OnboardingActivity" />

        <activity
            android:name=".patient.ConfirmPatientActivity"
            android:exported="false"
            android:label="@string/title_activity_confirm_patient"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".patient.SelectExistingPatientActivity"
            android:exported="false" />

        <activity
            android:name=".patient.RegistrationPatientActivity"
            android:exported="false" />

        <activity
            android:name=".video.VideoActivity"
            android:exported="false" />

        <activity
            android:name=".gallery.PreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.Fullscreen" />
        <activity
            android:name=".gallery.MediaPagerActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="sensorLandscape"
            android:exported="false" />
        <activity
            android:name=".gallery.MediaPagerActivityLand"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="false"
            android:screenOrientation="sensorLandscape"
            tools:ignore="DiscouragedApi" />
        <activity
            android:name=".gallery.SessionMediaActivity"
            android:exported="false" />
        <activity
            android:name=".settings.SystemInfoActivity"
            android:exported="false" />
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                tools:replace="android:resource" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>